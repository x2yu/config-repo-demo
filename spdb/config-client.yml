

spring:
    profiles: default
    application:
        name: config-client

server:
    port:1301
    
eureka:
    client:
        serviceUrl:
            defaultZone: http://localhost:2002/eureka/

info:
    des: 这是默认的默认的default
    author: x2yu